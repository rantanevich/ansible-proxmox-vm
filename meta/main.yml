---
galaxy_info:
  author: Raman Antanevich
  description: Creates virtual machines from template (cloud-init image)
  company: none

  license: MIT

  min_ansible_version: 2.9

  platforms:
    - name: Ubuntu
      versions:
        - bionic
        - focal
    - name: Centos
      versions:
        - 7
        - 8

  galaxy_tags:
    - proxmox
    - provision

dependencies:
  - role: geerlingguy.pip
    pip_install_packages:
      - name: proxmoxer
        version: 1.1.1
      - name: requests
        version: 2.22.0
