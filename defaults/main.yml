---
proxmox_api_host: localhost
proxmox_api_user: root@pam
proxmox_api_password: secret

proxmox_vm_default:
  node: pve
  sockets: 1
  cores: 2
  memory: 1024
  onboot: yes
  agent: yes
  balloon: 0

proxmox_volume_default: ""
